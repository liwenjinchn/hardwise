# Upstream API — Anthropic-format protocol via xiaomimimo.com proxy serving MiMo-V2.5
# (Anthropic SDK speaks the protocol; the model behind it is MiMo, not Claude.)
ANTHROPIC_API_KEY=replace_me
ANTHROPIC_BASE_URL=https://token-plan-sgp.xiaomimimo.com/anthropic

# Tiered model slots — three slots are an architectural pattern; if the upstream
# exposes only one model, all three may point to it. When MiMo ships per-tier
# variants, replace the corresponding value — no code change needed.
HARDWISE_MODEL_FAST=mimo-v2.5
HARDWISE_MODEL_NORMAL=mimo-v2.5
HARDWISE_MODEL_DEEP=mimo-v2.5

# Relational store — defaults to reports/<project>.db; no env var needed.
# Override the whole backend with any SQLAlchemy URL (optional):
# HARDWISE_DB_URL=postgresql+psycopg2://user@localhost:5432/hardwise
# Vector store dir is the --persist-dir CLI flag (ingest-datasheet /
# query-datasheet), not an env var.

# Optional public datasheet candidate lookup.
# Used only by `hardwise search-datasheets-com`; never expose this key in
# browser/static UI paths or generated reports.
DATASHEETS_API_KEY=replace_me
